name: Validate patch runner

on:
  push:
    branches:
      - fix/source-hud-retarget
    paths:
      - .github/workflows/apply-local-retarget.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo workflow-ok
